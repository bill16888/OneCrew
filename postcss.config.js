// Tailwind CSS v4: the PostCSS plugin moved to `@tailwindcss/postcss`
// and autoprefixer is built in, so it is no longer listed here.
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
